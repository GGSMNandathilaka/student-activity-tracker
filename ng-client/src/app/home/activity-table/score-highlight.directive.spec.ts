import { ScoreHighlightDirective } from './score-highlight.directive';

describe('ScoreHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ScoreHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
